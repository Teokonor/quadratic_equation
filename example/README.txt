This is example of project using library quadratic_equation.

In this directory (quadratic_equation/example) you can use that commands:
"make run" to build library and this project and run this project.
"make clean" to remove all builded files of that project.

This app infinitely ask you 3 args of quadratic equation and solve it when you have entered args.