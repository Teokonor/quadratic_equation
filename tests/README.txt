This is tests for library quadratic_equation.

In this directory (quadratic_equation/tests) you can use that commands:
"make run" to build library and tests and run tests.
"make clean" to remove all builded files of tests.